git config --global user.name "Nageswara Rao Thallapalli"
git config --global user.email "nagthallapalli@gmail.com"

git branch -m <old name> <new_name>

git rebase -i HEAD~6

git rebase -i <branch>

git branch -r   -> to see the list of branches available>

git diff --staged <file_name>  -> to see the diff of a file that was added to staging area.

